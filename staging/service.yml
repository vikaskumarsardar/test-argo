apiVersion: v1
kind: Service
metadata:
  name: stage-balancer
spec:
  selector:
    app: stage-service
  ports:
    - protocol: TCP
      port: 6789
      targetPort: 3456 # Use the port specified in your Node.js application
  type: LoadBalancer
